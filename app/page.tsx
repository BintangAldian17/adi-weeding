import Hero from "@/components/Hero";
import Opening from "@/components/Opening";

export default function Home() {
  return (
    <main className="w-full h-screen bg-primary overflow-hidden">
      <Hero isDefault />
    </main>
  );
}
